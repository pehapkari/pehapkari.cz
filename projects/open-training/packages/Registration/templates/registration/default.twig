{% extends 'base.html.twig' %}

{% block main %}
    <div id="trainings">
        <div class="section">
            <div class="container">
                <h2>Přihláška na školení {{ training.getName() }}</h2>
                <h3>({{ trainingTerm.getStartDateTime()|date("j. n. Y") }})</h3>

                <br>

                {{ form(form) }}
            </div>
        </div>
    </div>
{% endblock %}
