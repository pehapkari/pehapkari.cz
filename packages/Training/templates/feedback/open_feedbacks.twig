{% extends 'base.twig' %}

{% block main %}
    <div class="container">
        <h1 class="text-center">Dej nám feedback</h1>

        <p class="text-center bigger mb-5">
            Díky němu budou další školení, které navštívíš, lepší.
        </p>

        {# this alternative theme is also available #}
        {% form_theme form 'bootstrap_4_layout.html.twig' %}

        <div class="row">
            <div class="col-12 col-md-6">
                <div class="card shadow-card">
                    <div class="card-body card-form">
                        {{ form(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
