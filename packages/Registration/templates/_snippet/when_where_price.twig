{% if training_term is not null and training_term.registrationOpened() %}
    <h3>Deadline pro přihlášky?</h3>

    <p class="text-danger mb-5">
        {{ training_term.getDeadlineDateTime()|date }}
        (týden předem)
    </p>
{% endif %}

{% if training_term is not null %}
    <h3>Kdy?</h3>
    <p>
        {{ training_term.fromToHumanReadable }}
    </p>

    <p class="mb-5 mt-3">
        {# see https://github.com/spatie/calendar-links#}
        {# inspiration: https://www.meetup.com/Symfony-Usergroup-Chemnitz-Sudwestsachsen/events/259891562/ #}
        <a href="{{ training_term.googleCalendarLink }}" class="btn btn-sm btn-outline-primary">
            <em class="far fa-calendar-plus"></em>
            &nbsp;
            <em class="fab fa-google"></em>
        </a>

        <a href="{{ training_term.icalCalendarLink }}" class="btn btn-sm btn-outline-dark ml-2">
            <em class="far fa-calendar-plus"></em>
            &nbsp;
            <em class="fab fa-apple"></em>
        </a>
    </p>

    {#    https://www.meetup.com/Symfony-Usergroup-Chemnitz-Sudwestsachsen/events/259891562/ical/4.+Symfony+User+Group+Treffen+am+05.11.2019.ics#}

{#    <a href="https://www.google.com/calendar/render?action=TEMPLATE&text=Lecture:+{{ training.getName() }}&dates={{ training.getNearestTerm()|google_calendar_link }}&details=Details:+https://pehapkari.cz/{{ path('training_detail', { 'slug': training.getSlug() }) }}&ctz=Europe/Prague&location=U Pergamenky 1145/12, 170 00 Praha 7-Holešovice, Czechia&sf=true&output=xml">#}
{#        <em class="fa fa-fw fa-calendar"></em>#}
{#        Přidat do kalendáře#}
{#    </a>#}
{% endif %}

<h3>Kde?</h3>
<p class="mb-5">
    <a href="{{ path('place_detail') }}">Jablotron v Holešovicích, Praha</a>
</p>

<h3>Cena?</h3>
<p class="mb-5">
    {% if training_term is defined %}
        {{ training_term.price|number_format }} Kč
    {% else %}
        {{ training.price|number_format }} Kč
    {% endif %}
</p>
