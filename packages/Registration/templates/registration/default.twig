{% extends 'base.twig' %}

{% block main %}
    <div id="trainings">
        <div class="section">
            <div class="container">
                <h1 class="text-center">Přihláška na školení {{ training.getName() }}</h1>
                <h2>{{ trainingTerm.getStartDateTime()|date("j. n. Y H:i") }}</h2>

                <br>

                {# this alternative theme is also available #}
                {% form_theme form 'bootstrap_4_layout.html.twig' %}

                {{ form(form) }}
            </div>
        </div>
    </div>
{% endblock %}
