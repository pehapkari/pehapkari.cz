{% extends 'base.twig' %}

{% set page_title %}Chci na školení<br>"{{ training.getName() }}"{% endset %}

{% block main %}
    <div class="container" id="registration_form">
        <h1>{{ page_title }}</h1>

        {# table? #}
        <div class="row">
            <div class="col-6">
                <div class="card shadow-card">
                    <div class="card-body card-form">
                        {# this alternative theme is also available #}
                        {% form_theme form 'bootstrap_4_layout.html.twig' %}
                        {{ form(form) }}
                    </div>
                </div>
            </div>

            <div class="col-6 pl-5">
                {% include "_snippet/when_where_price.twig" %}
            </div>
        </div>
    </div>

    <br>
    <br>
{% endblock %}
