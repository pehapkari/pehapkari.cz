{% extends 'base.twig' %}

{% block main %}
    <div class="container" id="videos">
        <h1>Pusť si talk cestou do práce</h1>

        <div class="text-bigger black_underline_links">
            <p>
                Od prvního srazu přátel PHP v říjnu 2015 jsme úspěšně nahráli <strong>{{ video_count }}&nbsp;záznamů</strong> z <strong>{{ meetup_count }}&nbsp;srazů</strong>.
            </p>

            <p>
                K tomu ještě <a href="{{ path('videos_php_prague') }}">{{ php_prague_count }}&nbsp;přednášek z PHP Prague</a> a
                <a href="{{ path('livestream') }}">{{ livestream_count }}&nbsp;livestreamů</a>.
            </p>

            <p class="mt-3">
                Tady je najdeš <strong>všechny pěkně na jednom místě</strong>, od nejnovějších po nejstarší.
                <br>
                Jako bonus je pod každým videem i diskuse, tak se klidně ptej.
            </p>
        </div>

        <br>

        {% for playlist in meetup_playlists %}
            <h2 class="mt-5 mb-4" id="{{ playlist.title|webalize }}">{{ playlist.title }}</h2>

            <div class="row">
                {% for video in playlist.videos %}
                    {% include "_snippet/video_row.twig" %}
                {% endfor %}
            </div>

            <br>
        {% endfor %}
    </div>
{% endblock %}
